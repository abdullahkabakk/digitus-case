import type { Notification } from '@/types/notification'

// Define a mock notifications array
export const mockNotifications: Notification[] = [
  {
    icon: 'material-symbols:mail',
    count: 4
  },
  {
    icon: 'material-symbols:notifications-rounded',
    count: 12
  }
]
